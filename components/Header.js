// src/components/Header.js
import styles from './Header.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <div className={styles.text}>Our Blogs</div>
    </header>
  );
};

export default Header;
